specfile_path: packaging/rpm/systemd.spec
synced_files:
  - .packit.yaml
  - src: packaging/rpm/systemd.spec
    dest: systemd.spec
upstream_package_name: systemd
downstream_package_name: systemd
upstream_ref: v243-rhel8
patch_generation_ignore_paths:
- packaging/
- ci/
- .fmf/

jobs:
- job: copr_build
  trigger: pull_request
  metadata:
    targets:
    - fedora-30-x86_64
    - fedora-31-x86_64
    - fedora-rawhide-x86_64

- job: tests
  trigger: pull_request
  metadata:
    targets:
    - fedora-30-x86_64
    - fedora-31-x86_64
    - fedora-rawhide-x86_64
